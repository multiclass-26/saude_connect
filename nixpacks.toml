[phases.setup]
nixPkgs = ['python311', 'postgresql']

[phases.install]
cmds = ['/opt/venv/bin/pip install -r requirements.txt']

[phases.build]
cmds = ['python manage.py collectstatic --noinput']

[start]
cmd = 'python manage.py migrate && gunicorn config.wsgi'
